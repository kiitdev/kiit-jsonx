# Changelog

All notable changes to kiit-jsonx are documented here. Format follows
[Keep a Changelog](https://keepachangelog.com/), versions follow
[Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added
- Phase 1 (Foundation): `JsonxElement` tree type, `LexerState`/`SourcePosition`,
  `JsonxError`/`JsonxResult`/`JsonxException` error model, stubbed `ParseOptions`.
- Repo scaffolding modeled on `kiit-codes`: Gradle KMP module (JVM target only for Phase 1),
  ktlint/detekt, CI workflow, `samples/sample-kotlin`.
- Extracted from the Kiit framework as its own standalone module.
